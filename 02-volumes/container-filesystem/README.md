- Container filesystem behavior
- Stop vs remove
- The experiment we performed
- Why important data shouldn't live only in the container

# Important
- A running container gets a **writable layer** on top of the image.

- When you *create or modify files inside the container, those changes go into that writable layer.*









